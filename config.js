// ---------------------------------------------------------------------------
// Shared site configuration
// ---------------------------------------------------------------------------
// TODO (before going live): replace this with your deployed backend's real
// URL (e.g. "https://brendlo-api.onrender.com" or your own domain).
// "http://localhost:8080" only works while running the backend locally on
// your own machine — it will NOT work once this site is deployed to Render,
// since the browser has no way to reach your local machine from the internet.
// This single constant is shared by login.js, dashboard.js and
// forgot-password.js, so you only need to update it here.
// ---------------------------------------------------------------------------
const API_BASE_URL = "http://localhost:8080";
